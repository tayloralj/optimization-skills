interface:
  display_name: "Linux JVM Debug"
  short_description: "Route Linux JVM symptoms to safe debug evidence"
  default_prompt: "Use $linux-jvm-debug to turn this Linux JVM symptom into a safe evidence plan."
