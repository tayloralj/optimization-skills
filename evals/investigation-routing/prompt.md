Our Java service on Linux stops responding every few hours, but the process
stays alive. I can run read-only commands on this host as the service user.
I do not know whether this is GC, a deadlock, or something else. We have no
latency SLO yet. Help me start investigating without restarting the service.
Give a plan only; do not execute commands or invent a PID or measurements.
