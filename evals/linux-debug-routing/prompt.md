Use the top-level Linux JVM debug workflow for this request. Our Linux Java service has p99 latency spikes and occasional OOM kills. The
agent is not installed on the production host, and operations can only run
read-only commands as the service user. Give us the first safe steps and the
offline evidence plan. Do not restart the service or change kernel settings.
