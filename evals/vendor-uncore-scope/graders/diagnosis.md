---
type: llm
name: vendor-uncore-scope
---
Pass only if the response rejects per-JVM attribution from shared socket bandwidth, rejects method-level diagnosis from PCM alone, keeps Intel and AMD tools/events distinct, and proposes controlled same-host measurements with JVM-aware attribution. It must not equate bandwidth with saturation without a meaningful baseline or claim low bandwidth rules out memory-latency stalls.
